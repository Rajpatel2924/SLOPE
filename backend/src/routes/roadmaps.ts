import {Router} from "express";import {User,Roadmap} from "../models/models.js";import {requireAuth,AuthRequest} from "../middleware/auth.js";import {generateRoadmap} from "../services/engines.js";const r=Router();r.get("/me",requireAuth,async(req:AuthRequest,res,next)=>{try{const u:any=await User.findById(req.user!.id);if(!u?.activeCareerPathId)return res.status(400).json({success:false,message:"Choose a career path first"});let rm:any=await Roadmap.findOne({userId:req.user!.id,careerPathId:u.activeCareerPathId}).populate({path:"items.conceptId",populate:{path:"skillId"}});if(!rm)rm=await generateRoadmap(req.user!.id,String(u.activeCareerPathId));res.json({success:true,data:rm})}catch(e){next(e)}});export default r;
