import {createContext,useContext,useState} from "react";import {api,data} from "./api";const C=createContext<any>(null);export function AuthProvider({children}:{children:React.ReactNode}){const[user,setUser]=useState<any>(()=>{const x=localStorage.getItem("slope_user");return x?JSON.parse(x):null});const save=(d:any)=>{localStorage.setItem("slope_token",d.token);localStorage.setItem("slope_user",JSON.stringify(d.user));setUser(d.user)};const login=async(e:string,p:string)=>save(data(await api.post("/auth/login",{email:e,password:p})));const register=async(n:string,e:string,p:string)=>save(data(await api.post("/auth/register",{name:n,email:e,password:p})));const logout=()=>{localStorage.clear();setUser(null)};return <C.Provider value={{user,login,register,logout}}>{children}</C.Provider>}export const useAuth=()=>useContext(C);
