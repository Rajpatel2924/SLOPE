import {BrowserRouter,Routes,Route,Navigate} from "react-router-dom";import {AuthProvider,useAuth} from "./auth";import Layout from "./components/Layout";import Landing from "./pages/Landing";import {Auth} from "./pages/Auth";import Careers from "./pages/Careers";import Assessment from "./pages/Assessment";import Roadmap from "./pages/Roadmap";import Learning from "./pages/Learning";function P({children}:{children:React.ReactNode}){return useAuth().user?<>{children}</>:<Navigate to="/login"/>}export default function App(){return <AuthProvider><BrowserRouter><Layout><Routes><Route path="/" element={<Landing/>}/><Route path="/login" element={<Auth mode="login"/>}/><Route path="/register" element={<Auth mode="register"/>}/><Route path="/careers" element={<P><Careers/></P>}/><Route path="/assessment/:id" element={<P><Assessment/></P>}/><Route path="/roadmap" element={<P><Roadmap/></P>}/><Route path="/learning/:id" element={<P><Learning/></P>}/><Route path="*" element={<Navigate to="/"/>}/></Routes></Layout></BrowserRouter></AuthProvider>}
