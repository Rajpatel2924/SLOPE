import {Link,useNavigate} from "react-router-dom";import {useAuth} from "../auth";export default function Layout({children}:{children:React.ReactNode}){const{user,logout}=useAuth();const nav=useNavigate();return <><header className="border-b bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-4"><Link to="/" className="text-xl font-semibold">SLOPE<span className="text-neutral-400">.</span></Link><nav className="flex gap-5 text-sm">{user?<><Link to="/roadmap">Learning path</Link><button onClick={()=>{logout();nav("/login")}}>Logout</button></>:<><Link to="/login">Login</Link><Link to="/register" className="rounded-full bg-black px-4 py-2 text-white">Start learning</Link></>}</nav></div></header>{children}</>}
