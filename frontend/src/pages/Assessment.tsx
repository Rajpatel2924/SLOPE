import {useEffect,useState} from "react";import {useNavigate,useParams} from "react-router-dom";import {api,data} from "../api";export default function Assessment(){const{id}=useParams();const[d,setD]=useState<any>();const[a,setA]=useState<any>({});const nav=useNavigate();useEffect(()=>{api.get(`/assessments/baseline/${id}`).then(r=>setD(data(r)))},[id]);if(!d)return <div className="p-12">Loading...</div>;return <div className="mx-auto max-w-3xl px-6 py-16"><h1 className="text-4xl font-semibold">{d.title}</h1><p className="mt-3 text-neutral-600">Answer honestly; SLOPE uses this to find gaps.</p><div className="mt-10 space-y-7">{d.questions.map((q:any,i:number)=><div className="rounded-3xl border bg-white p-6" key={q._id}><p className="font-medium">{i+1}. {q.text}</p><div className="mt-4 space-y-2">{q.options.map((o:string,j:number)=><button key={j} onClick={()=>setA((v:any)=>({...v,[q._id]:j}))} className={`block w-full rounded-xl border p-3 text-left ${a[q._id]===j?"border-black bg-neutral-100":"border-neutral-200"}`}>{o}</button>)}</div></div>)}</div><button onClick={async()=>{await api.post(`/assessments/${d.assessmentId}/submit`,{answers:a});nav("/roadmap")}} className="mt-8 rounded-full bg-black px-7 py-3 text-white">Finish assessment</button></div>}
